export default function ClientsPage() {
  return (
    <div className="empty-state">
      <div className="empty-icon">→</div>
      <p>
        Select a client to view their projects, or add a new client to get
        started.
      </p>
    </div>
  );
}
